# FastAPI and server
fastapi>=0.109.0,<0.115.0
uvicorn[standard]>=0.27.0,<0.32.0

# File handling
python-multipart>=0.0.6
aiofiles>=23.2.1

# Validation
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.1.0,<3.0.0

# Speech-to-text (более гибкие версии)
faster-whisper>=1.0.0,<2.0.0

# Speaker diarization (используем последние совместимые версии)
pyannote.audio>=3.1.0,<4.0.0
# Pin huggingface_hub to version that supports use_auth_token for pyannote compatibility
huggingface-hub>=0.20.0,<0.21.0

# Audio processing - CUDA 12.2 compatible versions
# Install PyTorch with CUDA 12.1 support (compatible with CUDA 12.2 runtime)
# Use --index-url to get the correct CUDA version
torch==2.4.1
torchaudio==2.4.1
# NOTE: Install command in Dockerfile must use: pip install torch==2.4.1 torchaudio==2.4.1 --index-url https://download.pytorch.org/whl/cu121

# Utilities
numpy>=1.24.0,<2.0.0
scipy>=1.10.0,<1.15.0
